import React from 'react'
import ReactDOM from 'react-dom/client'
// import App from './App'
import EasyCardSimulator from './EasyCardSimulator'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    {/* <App /> */}
    { <EasyCardSimulator />}
  </React.StrictMode>,
)
